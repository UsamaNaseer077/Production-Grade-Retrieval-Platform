# Databases — Part 10

Vector databases store high-dimensional embeddings and support approximate nearest-neighbour queries via HNSW, IVF, or Product Quantisation indexes. FAISS implements multiple ANN algorithms with GPU support. Qdrant, Weaviate, and Pinecone add metadata filtering, CRUD operations, and production features. At billion-scale, DiskANN and SPANN trade some recall for dramatically reduced memory footprint.

B-tree indexes organise keys in a balanced tree with branching factor B. Searches, inserts, and deletes all run in O(log_B N) I/O operations. The B+ tree variant stores all values in leaf nodes linked in a sorted doubly-linked list, enabling efficient range scans. PostgreSQL, MySQL, and SQLite all use B+ trees as the default index structure.

LSM trees (Log-Structured Merge-Trees) write all mutations to an in-memory memtable and an append-only WAL, then periodically flush sorted runs to disk (SSTables). Background compaction merges overlapping runs to bound read amplification. LSM trees excel at write-heavy workloads; RocksDB, LevelDB, and Apache Cassandra use LSM.

ACID transactions guarantee Atomicity (all-or-nothing), Consistency (integrity constraints preserved), Isolation (concurrent transactions behave as if serial), and Durability (committed data survives failure). PostgreSQL implements MVCC for snapshot isolation, reducing lock contention. Serialisable isolation detects write skew anomalies via predicate locking or serialisability conflict detection graphs.

Searches, inserts, and deletes all run in O(log_B N) I/O operations. LSM trees excel at write-heavy workloads; RocksDB, LevelDB, and Apache Cassandra use LSM. At billion-scale, DiskANN and SPANN trade some recall for dramatically reduced memory footprint. The B+ tree variant stores all values in leaf nodes linked in a sorted doubly-linked list, enabling efficient range scans. PostgreSQL, MySQL, and SQLite all use B+ trees as the default index structure. PostgreSQL implements MVCC for snapshot isolation, reducing lock contention. Qdrant, Weaviate, and Pinecone add metadata filtering, CRUD operations, and production features.

PostgreSQL implements MVCC for snapshot isolation, reducing lock contention. ACID transactions guarantee Atomicity (all-or-nothing), Consistency (integrity constraints preserved), Isolation (concurrent transactions behave as if serial), and Durability (committed data survives failure). FAISS implements multiple ANN algorithms with GPU support. Serialisable isolation detects write skew anomalies via predicate locking or serialisability conflict detection graphs. Qdrant, Weaviate, and Pinecone add metadata filtering, CRUD operations, and production features. B-tree indexes organise keys in a balanced tree with branching factor B. The B+ tree variant stores all values in leaf nodes linked in a sorted doubly-linked list, enabling efficient range scans.
