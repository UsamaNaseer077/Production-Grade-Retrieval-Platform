# Databases — Part 8

LSM trees (Log-Structured Merge-Trees) write all mutations to an in-memory memtable and an append-only WAL, then periodically flush sorted runs to disk (SSTables). Background compaction merges overlapping runs to bound read amplification. LSM trees excel at write-heavy workloads; RocksDB, LevelDB, and Apache Cassandra use LSM.

SQL window functions compute aggregate values over a sliding partition of rows without collapsing them. PARTITION BY groups rows; ORDER BY sorts within the partition; the frame clause specifies which rows to include in each aggregate (ROWS BETWEEN, RANGE BETWEEN). Rank, dense_rank, row_number, lead, lag, and cumulative sum are common window functions.

Vector databases store high-dimensional embeddings and support approximate nearest-neighbour queries via HNSW, IVF, or Product Quantisation indexes. FAISS implements multiple ANN algorithms with GPU support. Qdrant, Weaviate, and Pinecone add metadata filtering, CRUD operations, and production features. At billion-scale, DiskANN and SPANN trade some recall for dramatically reduced memory footprint.

Database normalisation eliminates redundancy by decomposing tables into relations with minimal functional dependencies. First normal form requires atomic column values. Second normal form removes partial dependencies on composite primary keys. Third normal form removes transitive dependencies. BCNF (Boyce-Codd) is a stricter variant. Over-normalisation can hurt query performance; denormalisation trades space for read speed.

At billion-scale, DiskANN and SPANN trade some recall for dramatically reduced memory footprint. LSM trees (Log-Structured Merge-Trees) write all mutations to an in-memory memtable and an append-only WAL, then periodically flush sorted runs to disk (SSTables). Vector databases store high-dimensional embeddings and support approximate nearest-neighbour queries via HNSW, IVF, or Product Quantisation indexes. PARTITION BY groups rows; ORDER BY sorts within the partition; the frame clause specifies which rows to include in each aggregate (ROWS BETWEEN, RANGE BETWEEN). SQL window functions compute aggregate values over a sliding partition of rows without collapsing them. Qdrant, Weaviate, and Pinecone add metadata filtering, CRUD operations, and production features. Over-normalisation can hurt query performance; denormalisation trades space for read speed. First normal form requires atomic column values.

PARTITION BY groups rows; ORDER BY sorts within the partition; the frame clause specifies which rows to include in each aggregate (ROWS BETWEEN, RANGE BETWEEN). Over-normalisation can hurt query performance; denormalisation trades space for read speed. Vector databases store high-dimensional embeddings and support approximate nearest-neighbour queries via HNSW, IVF, or Product Quantisation indexes. LSM trees excel at write-heavy workloads; RocksDB, LevelDB, and Apache Cassandra use LSM. FAISS implements multiple ANN algorithms with GPU support. At billion-scale, DiskANN and SPANN trade some recall for dramatically reduced memory footprint. First normal form requires atomic column values. Background compaction merges overlapping runs to bound read amplification.
