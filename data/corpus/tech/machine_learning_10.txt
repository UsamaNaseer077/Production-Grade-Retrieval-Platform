# Machine Learning — Part 10

Overfitting occurs when a model achieves low training loss but high validation loss by memorising noise in the training set rather than learning generalisable patterns. Regularisation techniques include L1 and L2 weight penalties, dropout (randomly zeroing activations), data augmentation, early stopping, and ensembling. The bias-variance trade-off quantifies the tension between model underfitting (high bias) and overfitting (high variance).

Transformer architectures use self-attention to model long-range dependencies in sequences. The attention mechanism computes query-key dot products scaled by sqrt(d_k) to prevent gradient vanishing in deep networks. Multi-head attention runs h parallel attention functions whose outputs are concatenated and projected. BERT pretrains with masked language modelling and next sentence prediction on large unlabelled corpora.

Contrastive learning trains encoders to bring representations of similar pairs closer while pushing dissimilar pairs apart. SimCSE uses in-batch negatives and dropout as a natural augmentation. Hard negative mining from BM25 or a weaker retriever significantly improves retrieval quality. The InfoNCE loss is a practical implementation of maximum likelihood estimation over the contrastive objective.

Gradient descent minimises a loss function by computing the gradient with respect to all parameters and taking a step in the negative gradient direction. Stochastic gradient descent uses mini-batches to approximate the true gradient, reducing memory cost. Momentum, Adam, and AdaFactor are adaptive variants that adjust learning rates per-parameter based on gradient history, improving convergence on ill-conditioned objectives.

Overfitting occurs when a model achieves low training loss but high validation loss by memorising noise in the training set rather than learning generalisable patterns. Hard negative mining from BM25 or a weaker retriever significantly improves retrieval quality. Gradient descent minimises a loss function by computing the gradient with respect to all parameters and taking a step in the negative gradient direction. Transformer architectures use self-attention to model long-range dependencies in sequences. Stochastic gradient descent uses mini-batches to approximate the true gradient, reducing memory cost. The attention mechanism computes query-key dot products scaled by sqrt(d_k) to prevent gradient vanishing in deep networks. Multi-head attention runs h parallel attention functions whose outputs are concatenated and projected.

Gradient descent minimises a loss function by computing the gradient with respect to all parameters and taking a step in the negative gradient direction. Stochastic gradient descent uses mini-batches to approximate the true gradient, reducing memory cost. Overfitting occurs when a model achieves low training loss but high validation loss by memorising noise in the training set rather than learning generalisable patterns. The bias-variance trade-off quantifies the tension between model underfitting (high bias) and overfitting (high variance). Contrastive learning trains encoders to bring representations of similar pairs closer while pushing dissimilar pairs apart. Hard negative mining from BM25 or a weaker retriever significantly improves retrieval quality. Multi-head attention runs h parallel attention functions whose outputs are concatenated and projected.
