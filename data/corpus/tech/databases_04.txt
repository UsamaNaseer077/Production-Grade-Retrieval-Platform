# Databases — Part 4

SQL window functions compute aggregate values over a sliding partition of rows without collapsing them. PARTITION BY groups rows; ORDER BY sorts within the partition; the frame clause specifies which rows to include in each aggregate (ROWS BETWEEN, RANGE BETWEEN). Rank, dense_rank, row_number, lead, lag, and cumulative sum are common window functions.

ACID transactions guarantee Atomicity (all-or-nothing), Consistency (integrity constraints preserved), Isolation (concurrent transactions behave as if serial), and Durability (committed data survives failure). PostgreSQL implements MVCC for snapshot isolation, reducing lock contention. Serialisable isolation detects write skew anomalies via predicate locking or serialisability conflict detection graphs.

B-tree indexes organise keys in a balanced tree with branching factor B. Searches, inserts, and deletes all run in O(log_B N) I/O operations. The B+ tree variant stores all values in leaf nodes linked in a sorted doubly-linked list, enabling efficient range scans. PostgreSQL, MySQL, and SQLite all use B+ trees as the default index structure.

Database normalisation eliminates redundancy by decomposing tables into relations with minimal functional dependencies. First normal form requires atomic column values. Second normal form removes partial dependencies on composite primary keys. Third normal form removes transitive dependencies. BCNF (Boyce-Codd) is a stricter variant. Over-normalisation can hurt query performance; denormalisation trades space for read speed.

PostgreSQL implements MVCC for snapshot isolation, reducing lock contention. Searches, inserts, and deletes all run in O(log_B N) I/O operations. Serialisable isolation detects write skew anomalies via predicate locking or serialisability conflict detection graphs. PARTITION BY groups rows; ORDER BY sorts within the partition; the frame clause specifies which rows to include in each aggregate (ROWS BETWEEN, RANGE BETWEEN). B-tree indexes organise keys in a balanced tree with branching factor B. The B+ tree variant stores all values in leaf nodes linked in a sorted doubly-linked list, enabling efficient range scans. Over-normalisation can hurt query performance; denormalisation trades space for read speed. ACID transactions guarantee Atomicity (all-or-nothing), Consistency (integrity constraints preserved), Isolation (concurrent transactions behave as if serial), and Durability (committed data survives failure).

Third normal form removes transitive dependencies. PostgreSQL, MySQL, and SQLite all use B+ trees as the default index structure. SQL window functions compute aggregate values over a sliding partition of rows without collapsing them. B-tree indexes organise keys in a balanced tree with branching factor B. Second normal form removes partial dependencies on composite primary keys. PARTITION BY groups rows; ORDER BY sorts within the partition; the frame clause specifies which rows to include in each aggregate (ROWS BETWEEN, RANGE BETWEEN). Serialisable isolation detects write skew anomalies via predicate locking or serialisability conflict detection graphs. The B+ tree variant stores all values in leaf nodes linked in a sorted doubly-linked list, enabling efficient range scans.
