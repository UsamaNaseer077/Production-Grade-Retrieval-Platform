# Databases — Part 1

LSM trees (Log-Structured Merge-Trees) write all mutations to an in-memory memtable and an append-only WAL, then periodically flush sorted runs to disk (SSTables). Background compaction merges overlapping runs to bound read amplification. LSM trees excel at write-heavy workloads; RocksDB, LevelDB, and Apache Cassandra use LSM.

SQL window functions compute aggregate values over a sliding partition of rows without collapsing them. PARTITION BY groups rows; ORDER BY sorts within the partition; the frame clause specifies which rows to include in each aggregate (ROWS BETWEEN, RANGE BETWEEN). Rank, dense_rank, row_number, lead, lag, and cumulative sum are common window functions.

Vector databases store high-dimensional embeddings and support approximate nearest-neighbour queries via HNSW, IVF, or Product Quantisation indexes. FAISS implements multiple ANN algorithms with GPU support. Qdrant, Weaviate, and Pinecone add metadata filtering, CRUD operations, and production features. At billion-scale, DiskANN and SPANN trade some recall for dramatically reduced memory footprint.

ACID transactions guarantee Atomicity (all-or-nothing), Consistency (integrity constraints preserved), Isolation (concurrent transactions behave as if serial), and Durability (committed data survives failure). PostgreSQL implements MVCC for snapshot isolation, reducing lock contention. Serialisable isolation detects write skew anomalies via predicate locking or serialisability conflict detection graphs.

SQL window functions compute aggregate values over a sliding partition of rows without collapsing them. Rank, dense_rank, row_number, lead, lag, and cumulative sum are common window functions. Background compaction merges overlapping runs to bound read amplification. At billion-scale, DiskANN and SPANN trade some recall for dramatically reduced memory footprint. PARTITION BY groups rows; ORDER BY sorts within the partition; the frame clause specifies which rows to include in each aggregate (ROWS BETWEEN, RANGE BETWEEN). Qdrant, Weaviate, and Pinecone add metadata filtering, CRUD operations, and production features.

FAISS implements multiple ANN algorithms with GPU support. LSM trees (Log-Structured Merge-Trees) write all mutations to an in-memory memtable and an append-only WAL, then periodically flush sorted runs to disk (SSTables). ACID transactions guarantee Atomicity (all-or-nothing), Consistency (integrity constraints preserved), Isolation (concurrent transactions behave as if serial), and Durability (committed data survives failure). Serialisable isolation detects write skew anomalies via predicate locking or serialisability conflict detection graphs. PARTITION BY groups rows; ORDER BY sorts within the partition; the frame clause specifies which rows to include in each aggregate (ROWS BETWEEN, RANGE BETWEEN). LSM trees excel at write-heavy workloads; RocksDB, LevelDB, and Apache Cassandra use LSM.
